---
title: 'Content Presentation'
description: 'About Content Page Rendering'
order: 5
---

## Shiki Code

You can change transformer at `defineConfig.markdown.shikiConfig` in config file `astro.config.ts`.

### Basic Usage

For `.astro` file:

```astro
---
import { Code } from 'astro:components'
---

<Code lang='shell' code={`git log --oneline`} />
```

For `.mdx` and `.md` file:

````markdown
```shell
git log --oneline
```
````

### Add Title

````markdown
```shell title="start.sh"
#!/bin/bash

bun check &
bun dev
```
````

### Show Diff

Supports add and del lines.

````markdown
```shell title="deploy.sh"
#!/bin/bash

bun check
bun lint
bun format # [!code ++]
# No need for vercel or other ci/cd
bun run build # [!code --]
git push
```
````
